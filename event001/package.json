{
  "name": "event001",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ event001",
    "create-local": "graph create --node http://localhost:8020/ event001",
    "remove-local": "graph remove --node http://localhost:8020/ event001",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 event001"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
